# 产品雷达（Product Radar）开发文档

**版本：** v2.2
**日期：** 2026-07-28
**原则：** 产品机会数据库、完整调研、AI 决策

---

## 1. 产品定义

产品雷达是一个面向 Web 和 iOS 的产品机会数据库与决策系统。

它长期积累并回答三个问题：

1. 我已经上线了哪些产品？
2. 雷达目前发现了哪些产品机会？
3. 我下一步最值得开发什么产品？

它不是通用 SEO 工具，也不是复杂的数据平台。

它既要保存完整的产品机会库，也要从数据库中持续筛选出可以立即采取行动的开发建议。

可以简化的是数据库、页面、部署、自动化和后台管理。

不能简化的是数据积累和“这个产品值不值得开发”的判断。进入最终推荐的每个机会，都必须经过多维度真实数据调研和 AI 综合判断，而不是由几个简单阈值直接决定。

### 成功体验

用户每天打开首页，30 秒内得到答案：

```text
下一步最推荐：

1. Image Metadata Remover       88 分
   需求明确、竞争较弱、预计 1 天、可纯前端完成

2. CSV Column Extractor         84 分
   搜索稳定、工具意图强、与已有产品不重复

3. Photo GPS Checker            81 分
   来自已有产品的相邻需求，适合增加新页面
```

同时能看到：

```text
已上线 12    开发中 2    待开发 4    表现增长 3
```

---

## 2. 产品结构

第一版只有三个主要入口：

```text
首页：下一步做什么
雷达库：发现过哪些机会和数据
产品库：已经做过什么
```

### 2.1 首页：下一步推荐

展示排名第一的产品机会：

- 产品名称；
- 推荐分；
- AI 结论和置信度；
- 三条推荐理由；
- 预计开发时间；
- 是否需要后端；
- 是否需要收费 API；
- 推荐做成独立产品、已有产品功能或 Landing Page；
- “查看详情”；
- “加入开发”。

### 2.2 首页：高优先级机会

字段：

```text
排名
产品名称
推荐分
AI 结论
调研置信度
核心证据
预计开发时间
实现方式
推荐形态
状态
```

首页只展示一小部分高优先级摘要，默认数量可以配置；系统不会只保存这些结果。

用户可以点击“查看全部”，进入完整雷达库。

### 2.3 首页：我的产品摘要

展示：

- 待开发；
- 开发中；
- 已上线；
- 观察中；
- 持续增长；
- 停止投入。

每个产品只显示名称、平台、状态、网站或 App Store 地址、主关键词、上线时间和最近表现。

页面底部显示最近一次数据更新时间、运行状态和本次成本。只有失败时才突出告警。

### 2.4 雷达库

雷达库展示系统发现并保存的全部 Opportunity，而不只是高分结果。

列表支持：

- 按总分和各维度分排序；
- 按 Web、iOS、Web+iOS 筛选；
- 按最终结论、调研状态、来源和日期筛选；
- 按搜索量、开发时间、置信度筛选；
- 搜索产品名称和关键词；
- 分页；
- 选择每页 20、50 或 100 条；
- 点击进入完整调研报告。

建议列表字段：

```text
名称
推荐平台
最终结论
总分
分数变化
置信度
搜索需求
竞争
开发难度
推荐形态
调研状态
更新时间
```

支持按“最近涨分最多”和“最近新进入高优先级”排序。

未完成深度调研的候选也保留在雷达库中，但必须显示 `RESEARCH_PENDING` 或 `PARTIAL`，不能和已完成调研的结果混在一起。

### 2.5 数据展示

系统采集到的数据都应可查看，不只保存在后台。

Opportunity 详情中用标签页或折叠区展示：

```text
调研结论
关键词与趋势
Web SERP
App Store 搜索结果
竞品
用户评论与痛点
评分明细
原始证据
历史调研版本
```

原始证据允许分页展示，并标明来源、地区、语言和采集时间。

### 2.6 产品库

产品库展示全部 Web 和 iOS 产品，支持平台、状态和上线时间筛选，并支持分页。

---

## 3. 用户闭环

```text
系统发现需求
→ 生成并排序产品机会
→ 用户选择一个机会
→ 转为待开发产品
→ 开发并上线
→ 系统记录已做过
→ 后续不再重复推荐
```

系统还支持三条主动输入流程：

```text
输入一个产品点子
→ AI 制定调研计划
→ 补齐 Web / App Store /竞品 / 用户痛点数据
→ 输出是否值得做和适合的平台

添加一个现有产品
→ 纳入产品库
→ 提取能力和关键词
→ 用于重复检测、功能建议和相邻机会发现

导入一批用户抱怨
→ 形成 Signal
→ AI 聚类重复痛点
→ 关联已有产品或生成新 Opportunity
→ 进入完整调研和判断流程
```

用户对机会只有五种操作：

```text
加入开发
加入观察
作为已有产品的新功能
作为新的 Landing Page
忽略
```

点击“加入开发”后：

- 创建 Product；
- Opportunity 标记为已选择；
- 相似机会不再作为全新产品高优先推荐。

产品上线后，用户手工填写：

- 平台：Web、iOS 或 Web+iOS；
- 网站地址；
- App Store 地址；
- 上线时间；
- 主关键词；
- 可选 Repo 地址。

第一版不需要 GitHub 或部署平台集成。

---

## 4. 雷达数据库的核心对象

### Product

代表已经决定开发或已经上线的产品。

平台：

```text
WEB
IOS
WEB_AND_IOS
```

状态：

```text
TODO
BUILDING
DEPLOYED
OBSERVING
GROWING
MAINTAIN
ABANDONED
```

### Opportunity

代表一个去重后的产品建议。

例如：

```text
Image Metadata Remover
```

可以关联：

```text
jpg metadata remover
jpeg exif remover
remove metadata from photo
```

三个搜索词只能形成一个产品机会。

每个 Opportunity 都需要判断：

- 是否值得开发；
- 最适合 Web、iOS 还是同时开发；
- 是否应该独立开发、加入现有产品或做成 Landing Page；
- 当前调研是否完整。

### Signal

代表一条可能包含产品机会的信息。

Signal 可以来自：

```text
手工输入的产品点子
手工添加的已有产品
搜索关键词
Web SERP
App Store 搜索和评论
Reddit / X / 论坛中的抱怨
用户反馈或访谈
CSV / JSON 导入
Search Console 新 query
```

Signal 本身不等于 Opportunity。

系统需要判断它是：

- 噪声；
- 已有 Opportunity 的补充证据；
- 已有 Product 的改进建议；
- 可以形成新 Opportunity 的重复痛点；
- 暂时证据不足，需要观察。

系统内部可以记录 Discovery Run，用于显示一次任务是否成功、生成多少机会和花费多少，但它不是核心产品模块。

---

## 5. 功能架构

系统按照职责拆成七个模块。模块之间通过结构化数据连接，避免采集、判断和展示混在一起。

```text
输入源
→ 信号收件箱
→ 数据采集与标准化
→ 候选机会生成
→ 深度调研
→ AI 判断与评分
→ 雷达数据库
→ 结果展示与人工操作
             ↑
        数据更新与重评
```

### 5.1 信号收件箱

负责接收一切外部输入：

- 手工填写一个产品点子；
- 手工添加一个现有 Web 或 iOS 产品；
- 粘贴一段 Reddit、X、论坛或用户评论；
- 输入一个公开链接；
- 上传 CSV 或 JSON；
- 接收关键词、App Store、Search Console 等自动采集结果。

每条 Signal 保存来源、原文、链接、时间、平台和处理状态。

状态：

```text
NEW
CLASSIFIED
ATTACHED_TO_OPPORTUNITY
ATTACHED_TO_PRODUCT
OPPORTUNITY_CREATED
IGNORED
```

第一版优先支持手工输入、链接和 CSV，不把 Reddit 或 X 的自动 API 接入作为阻塞项。

社区数据只保存调研需要的公开内容、来源和聚合指标，不保存不必要的个人身份信息。数据采集和保留必须遵守来源平台的访问条款。

### 5.2 数据采集

只负责获取和保存事实，不负责判断值不值得做。

包括：

- 搜索关键词、搜索量和趋势；
- Web SERP；
- App Store 搜索、App 信息、评分和评论；
- 公开竞品页面；
- 用户手工提供的评论和抱怨；
- 后续的 Search Console 数据。

所有数据统一转换为 `evidence_items`，保存来源和采集时间。

### 5.3 信号处理与候选生成

负责：

- 清洗和去重；
- 识别语言、对象、动作和用户任务；
- 将相似抱怨聚成痛点主题；
- 匹配已有 Product 和 Opportunity；
- 判断 Signal 是补充证据还是新候选；
- 为新候选创建 `RESEARCH_PENDING` Opportunity。

单条抱怨不能直接生成高分机会。系统要考虑重复频率、参与度、痛点严重性、场景一致性和是否存在付诸行动的意图。

### 5.4 深度调研

为 Opportunity 建立研究计划，补齐搜索、竞品、用户痛点、平台和开发可行性证据。

它只输出事实报告和未知项，不直接负责最终排名。

### 5.5 AI 判断与评分

读取完整证据包，完成正反论证、平台判断、评分、置信度和最终建议。

判断结果必须形成新的 `research_report` 版本。

### 5.6 雷达数据库

长期保存：

- 所有 Product；
- 所有 Opportunity；
- 所有 Signal；
- 全部证据；
- 当前评分；
- 历史评分；
- AI 判断版本；
- 人工决策和产品结果。

它是产品的核心资产，不只是一个 Top 推荐结果缓存。

### 5.7 结果展示与人工操作

负责首页、雷达库、产品库、信号收件箱、详情报告、排序筛选、分页和人工状态管理。

人工可以：

- 添加产品；
- 添加点子；
- 导入抱怨数据；
- 要求某个候选立即调研；
- 将 Signal 关联到已有机会；
- 接受或否决 AI 判断；
- 把 Opportunity 转成 Product。

---

## 6. Opportunity 详情就是调研报告

详情页不是简单的数据卡片，而是一份“是否值得开发”的完整决策报告。

### 6.1 最终结论

必须明确给出：

```text
BUILD_NOW       值得立即开发
VALIDATE_FIRST  有潜力，但需要先验证关键问题
WATCH           保留观察
SKIP            不值得投入
```

同时展示：

- 推荐分；
- AI 判断置信度；
- 推荐做成新产品、已有产品功能或 Landing Page；
- 三条最强支持证据；
- 三条最主要反对理由；
- 当前仍不确定的问题。

### 6.2 多维度调研

每个最终候选至少研究以下维度：

1. **搜索需求**：搜索量、相关词规模、12 个月趋势和需求稳定性；
2. **用户任务与痛点**：用户到底想完成什么、问题是否具体、是否急迫；
3. **搜索意图**：用户是在学习、比较，还是明确想使用一个工具；
4. **Web 搜索竞争**：现有网页是否精确满足需求、是否被强品牌垄断；
5. **App Store 竞争**：相关 App 的排名、评分、评论量、价格、更新频率和评论痛点；
6. **差异化空间**：能否在速度、隐私、本地处理、易用性或细分场景上更好；
7. **平台适配度**：最适合 Web、iOS 还是 Web+iOS，移动端是否具有独特价值；
8. **开发可行性**：预计工时、前后端需求、iOS 原生能力、第三方 API、审核与运行成本；
9. **获客与扩展**：Web SEO、App Store 搜索、相邻关键词、Landing Page、功能或产品组合；
10. **商业与战略价值**：付费、订阅、内购、广告、导流可能性，以及是否适合现有产品方向；
11. **内部重复度**：是否已经做过，应该新建、加功能还是加页面。

### 6.3 证据要求

AI 的每个重要结论必须引用对应证据：

- DataForSEO 指标要带数据时间和地区；
- SERP 结论要能回到具体结果；
- 竞品判断要能回到具体 URL、标题、功能或定价页面；
- 推测必须标记为推测，不能伪装成事实；
- 证据不足时只能给 `VALIDATE_FIRST` 或低置信度，不能强行推荐。

### 6.4 产品建议

报告最后给出：

- 目标用户；
- 核心任务；
- 建议产品定位；
- MVP 功能；
- 明确不做什么；
- 预计开发时间；
- 推荐平台以及选择该平台的原因；
- 如果同时支持 Web 和 iOS，说明共用能力和平台差异；
- 推荐验证方式；
- 推荐形态：

```text
BUILD_NEW_PRODUCT
ADD_FEATURE_TO_EXISTING
CREATE_LANDING_PAGE
WATCH
SKIP
```

平台建议：

```text
WEB
IOS
WEB_AND_IOS
```

---

## 7. 推荐如何产生

### 输入

第一版使用 10～20 个简单 Seed：

```text
image
pdf
csv
metadata
converter
checker
remover
generator
```

Seed 放在一个简单设置页或本地配置文件中。Seed 只负责发现候选，不能直接决定候选是否值得开发。

### 流程

```text
1. 从 Seed 获取大量候选关键词
2. 规则只做去噪、合规过滤和明显重复归并
3. AI 识别真实用户任务，并形成候选产品组
4. 为候选组建立调研计划
5. 获取搜索量、趋势、相关词和多组 Web SERP
6. 对适合 iOS 的候选获取 App Store 搜索、App 信息和用户评论
7. 调研现有工具、替代方案、用户痛点和市场缺口
8. AI 分别生成支持理由、反对理由和未知问题
9. AI 基于完整证据包判断是否值得做以及适合的平台
10. 与已有 Opportunity 和 Product 比较
11. 保存全部候选、证据和研究状态
12. 首页展示当前高优先级机会，雷达库展示全部数据
```

### AI 判断流程

每个进入最终评审的候选至少经过三次独立步骤：

#### 第一步：研究员

只提取事实、整理证据和指出缺失信息，不做最终推荐。

#### 第二步：正反论证

分别回答：

- 为什么值得做；
- 为什么不值得做；
- 最可能导致失败的原因是什么；
- 还需要验证什么。

#### 第三步：决策者

读取事实和正反论证，输出最终建议、各维度评分、置信度和推荐动作。

三个步骤可以使用同一个模型，但必须使用分开的结构化调用，不能用一次 Prompt 同时完成所有判断。

### 成本控制

可以限制进入深度调研的候选数量，但不能降低最终候选的调研深度。

先从大量关键词筛到约 20～30 个产品候选，再对这些候选做完整调研。

默认上限：

```text
每次运行 Seed：最多 20 个
每个 Seed 保留候选：最多 100 个
进入 AI 初步聚类：最多 100 组
进入深度调研：最多 20～30 个
每个候选 SERP/补充查询：3～6 组
全部候选：保存到雷达库
完成深度调研：优先 20～30 个
首页默认展示：可配置，例如前 10 个
完整列表：排序、筛选、分页查看
```

如果预算不足，应减少深度调研的候选数量，而不是让最终推荐使用不完整数据。没有完成深度调研的候选继续保留在雷达库，等待后续更新。

---

## 8. 判断系统

### 8.1 规则的边界

规则只允许用于：

- 去除成人、违法、品牌导航等明显不合适的内容；
- 过滤完全没有数据或与方向无关的噪声；
- 规范化关键词；
- 控制请求数量和预算；
- 找出明显重复项。

规则不能直接输出“值得做”或“不值得做”。

所有进入最终排名的候选都必须经过 AI 深度判断。

### 8.2 去重判断

去重使用规则加 AI：

1. 规则识别单复数、jpg/jpeg、相同对象和高度相似字符串；
2. AI 判断用户任务是否真正相同，以及应该合并、加功能还是新增页面。

例如：

```text
image metadata viewer
image metadata remover
```

对象相同，但任务不同，不能合并。

第一版不使用向量数据库。当 Opportunity 超过约 1,000 个并出现明显性能问题时，再考虑 Embedding 和 pgvector。

### 8.3 多维度评分

| 维度 | 分数 | 主要证据 |
|---|---:|---|
| 搜索需求与趋势 | 15 | 搜索量、相关词规模、12 个月和近期变化 |
| 用户任务与痛点 | 15 | 查询表达、讨论、现有解决方式 |
| Web/App Store 可进入性 | 15 | Web SERP、App 排名、评分、评论量和结果质量 |
| 竞品缺口与差异化 | 15 | 功能、价格、体验、隐私和细分场景 |
| 平台适配度 | 10 | Web、iOS 或双平台的独特价值与分发条件 |
| 开发与运行可行性 | 10 | 工时、技术难度、后端和 API 成本 |
| 获客与扩展空间 | 10 | 相邻关键词、页面和功能扩展 |
| 商业价值 | 5 | 付费、广告、导流或长期价值 |
| 产品组合与内部信号 | 5 | 已有产品关系、重复度、真实反馈 |
| **总分** | **100** | |

每个维度必须同时保存：

```text
score
reason
evidence_ids
confidence
unknowns
```

### 8.4 分数不替代 AI 结论

加权分数用于比较候选，但最终判断由 AI 根据完整研究报告产生。

AI 允许在发现致命风险时否决一个高分候选，也允许把数据暂时不足但潜力较大的候选标记为 `VALIDATE_FIRST`。

最终输出必须包含：

- 正面论证；
- 反面论证；
- 最可能失败的原因；
- 关键未知项；
- 最终决定；
- 置信度；
- 哪些新证据会改变当前决定。

如果数据相互矛盾，系统必须展示矛盾，而不是强行得出确定结论。

### 8.5 平台判断

除了总分，每个机会还要分别输出：

```text
web_fit_score
ios_fit_score
cross_platform_score
recommended_platform
platform_reason
```

判断 iOS 价值时必须考虑：

- 是否需要相机、照片、文件、通知、定位、离线处理等原生能力；
- 移动端使用是否比网页更自然；
- App Store 是否已有大量高质量强竞品；
- 竞品评论中是否存在持续未解决的痛点；
- 是否适合订阅、内购或一次性付费；
- 审核、隐私权限和长期维护成本。

判断 Web 价值时必须考虑：

- SEO 是否是主要获客方式；
- 用户是否希望无需安装立即使用；
- 任务是否适合浏览器本地处理；
- 是否适合通过多个 Landing Page 获取流量。

---

## 9. 最小技术方案

技术只负责保存和展示研究结果，不是产品重点。

第一版可以全部运行在一台机器上：

```text
Web：Next.js + TypeScript
UI：Tailwind CSS + shadcn/ui
数据：SQLite
调研任务：同仓库 Node.js 脚本
原始证据：本地 JSON 文件
触发方式：先手工按钮，稳定后再加 Cron
```

如果后续需要多人访问或数据量明显增长，再把 SQLite 换成 PostgreSQL。业务设计不能依赖具体数据库。

第一版不需要独立 Worker 服务、Python、Redis、Celery、Kafka、Kubernetes、微服务或复杂部署。

### 雷达库数据结构

#### products

```text
id
name
platform
status
description
primary_keyword
website_url
app_store_url
app_store_id
repo_url
source_opportunity_id
started_at
launched_at
notes
```

#### opportunities

```text
id
name
status
research_status
source
primary_keyword
recommended_platform
web_fit_score
ios_fit_score
cross_platform_score
recommended_action
current_score
previous_score
score_delta
confidence
latest_research_version
similar_product_id
human_verdict
human_notes
first_seen_at
last_researched_at
created_at
updated_at
```

列表需要排序和筛选的字段必须保存为独立列，不能全部塞进 JSON。

#### signals

保存所有手工或自动输入：

```text
id
signal_type
source
source_url
raw_text
metadata JSON
platform
status
related_product_id
related_opportunity_id
created_at
processed_at
```

`signal_type` 包括：

```text
IDEA
PRODUCT
KEYWORD
REDDIT_POST
REDDIT_COMMENT
X_POST
FORUM_POST
APP_REVIEW
USER_FEEDBACK
GSC_QUERY
IMPORT
```

#### keywords

保存全部已发现关键词及其最新指标：

```text
keyword
country
language
search_volume
trend
cpc
source
first_seen_at
last_updated_at
```

#### opportunity_keywords

保存 Opportunity 与多个关键词的关联。

#### evidence_items

统一保存所有原始和结构化证据：

```text
opportunity_id
evidence_type
platform
source
source_url
source_ref
data JSON
country
language
collected_at
expires_at
```

`evidence_type` 包括：

```text
KEYWORD_METRIC
TREND
WEB_SERP
COMPETITOR_PAGE
APP_STORE_SEARCH
APP_INFO
APP_REVIEW
USER_PAIN
INTERNAL_PRODUCT_SIGNAL
```

#### research_reports

每次重新调研都新增一条版本，不覆盖历史：

```text
opportunity_id
version
score_model_version
prompt_version
score
score_breakdown JSON
ai_judgment JSON
recommended_platform
recommended_action
confidence
change_summary
created_at
```

#### discovery_runs

保存开始时间、结束时间、状态、候选数量、机会数量、成本和错误。

#### settings

保存 Seed、国家、语言、数量上限、预算、过滤词和评分权重。

证据中必须保存来源、抓取时间和引用标识，确保 AI 结论可以追溯。

第一版仍然可以使用 SQLite。重点是形成可查询、可分页、可更新、可追溯的产品机会数据库，而不是选择更复杂的数据库产品。

---

## 10. 数据更新与重新评分

Opportunity 的分数和结论不是永久结果，而是某个时间点基于当时证据得出的判断。

系统同时保存：

```text
当前评分与当前结论
历史评分与历史结论
每次使用的证据快照
评分模型和 Prompt 版本
变化原因
```

### 10.1 更新来源

以下变化都可能触发重新评分：

- 搜索量或趋势明显变化；
- Web SERP 发生变化；
- App Store 排名、评分、评论量或价格变化；
- 竞品发布、下架、改价或长期没有更新；
- 用户评论出现新的集中痛点；
- 新产品加入产品库，需要重新判断重复度；
- Search Console 出现新 query 或增长信号；
- 增加新的评分维度；
- 修改评分权重、研究 Prompt 或 AI 模型；
- 用户手工要求重新调研。

### 10.2 更新频率

建议默认：

```text
趋势异常信号：每天
App Store 排名、评分和评论量：每天或每 3 天
高优先级机会的 Web SERP：每 7 天
普通机会的 Web SERP：每 30 天
月度搜索量：每 30 天
竞品页面和价格：每 14～30 天
Search Console：每天
完整 AI 重评：证据发生实质变化时
```

具体频率可以按成本调整。

月度搜索量本身无法反映第二天的突发上涨，因此短期变化应额外使用趋势数据。趋势只代表相对热度变化，不能冒充精确搜索量。

### 10.3 增量更新

每次更新流程：

```text
1. 获取新证据
2. 与上一份证据快照比较
3. 找出受影响的 Opportunity
4. 只重新调研发生变化的维度
5. AI 读取完整证据包重新判断
6. 创建新的 research_report 版本
7. 更新 Opportunity 当前分数和结论
8. 保存分数变化和原因
```

例如：

```text
72 → 86（+14）

变化原因：
- 最近 7 天趋势明显上升
- App Store 头部竞品评分从 4.5 降至 3.8
- 新增评论集中抱怨隐私和导出体验
- iOS 平台适配分从 62 提升到 84
```

### 10.4 新增评分维度

当系统增加一个新的研究维度时：

- 创建新的 `score_model_version`；
- 使用新维度重新计算全部已完成调研的 Opportunity；
- 不覆盖旧评分；
- 列表默认展示最新版本；
- 详情页可以比较不同版本的分数和结论。

### 10.5 数据新鲜度

每条 Opportunity 显示：

```text
FRESH
STALE
UPDATING
PARTIAL
FAILED
```

排名默认只使用 `FRESH` 且完成深度调研的结果。过期或部分数据仍保留在雷达库中，但必须明显标记。

---

## 11. 外部 API

### 第一版必须

#### DataForSEO

用于：

- Keyword Suggestions；
- Keyword Ideas；
- 搜索量、CPC 和月度搜索趋势；
- DataForSEO Trends 的短期相对热度；
- 主关键词的 Google Organic SERP；
- 竞品、替代方案、用户问题等补充查询的 SERP；
- Apple App Store 搜索结果；
- App 信息、评分、价格和评论；
- App Store 用户评论。

优先使用 Suggestions 和 Ideas 已返回的指标，不重复查询。

每个深度调研候选可以补充查询：

```text
主任务词
best / alternative + 主任务词
how to + 主任务词
主任务词 + reddit / forum
相邻任务词
```

不要求机械地查询全部模板，由 AI 根据证据缺口生成 3～6 个必要查询。

竞品功能和定价可以读取公开页面；若页面无法访问，报告必须明确标记缺失，不能猜测。

SERP 和 App Data 任务优先使用 Standard Queue，手工立即刷新才使用高优先级或 Live。

#### LLM Provider

用于：

- 识别关键词背后的用户任务；
- 生成调研问题和补充搜索计划；
- 从搜索结果与公开页面提取结构化证据；
- 调研竞品、替代方案、痛点和差异化空间；
- 生成正面和反面论证；
- 判断边界重复；
- 给出最终开发建议、置信度、MVP 和预计工时。

所有输出必须是经过 Schema 校验的结构化 JSON，并引用输入证据 ID。模型不得把自身常识当作已经调研到的事实。

#### 手工数据输入

第一版必须支持：

- 粘贴文本；
- 输入公开 URL；
- 上传 CSV/JSON；
- 指定 Signal 类型和来源；
- 将数据关联到某个 Product 或 Opportunity。

这样即使 Reddit 或 X API 暂时不可用，用户仍然可以把发现的抱怨和评论纳入研究。

### 第二阶段

第一批产品上线并获得数据后，再接 Google Search Console：

```text
上线产品
→ 获取真实 query、impressions 和 clicks
→ 发现相邻需求
→ 生成下一批产品机会
```

还可以按价值增加：

- X API 最近帖子搜索；
- Reddit 官方 API 或获批的数据接口；
- 用户反馈表单；
- App Store 评论定期同步；
- 其他论坛和社区连接器。

任何社区数据连接器都只是 Signal 来源，不能绕过统一的调研和 AI 判断流程。

### 第一版不接

- Google 官方 Trends API；
- Google Ads；
- GitHub App/Webhook；
- Vercel/Cloudflare API；
- Google Analytics；
- 收入和通知平台。

---

## 12. 开发计划

### A. 结果验证版

预计 10～14 个工作日。

完成：

- 手工录入 Web/iOS Product、产品点子和评论 Signal；
- Signal 收件箱和基础分类；
- 配置 Seed；
- DataForSEO 真实调用；
- 规则去噪和 AI 产品任务聚类；
- Web SERP、App Store 搜索、App 信息和评论；
- 竞品、替代方案和跨平台用户痛点调研；
- AI 研究员、正反论证、最终决策三阶段判断；
- 多维评分、平台判断、证据引用和置信度；
- 首页高优先级摘要；
- 完整雷达库的排序、筛选和分页；
- Opportunity 完整调研报告。

验收：

```text
输入 10～20 个 Seed
或手工输入一个产品点子/一批用户抱怨
→ 从多来源数据形成产品候选
→ 为最终候选完成多维度真实调研
→ 生成至少 10 份不重复、可追溯的产品决策报告
→ 每份报告有 Web/iOS 平台判断、支持理由、反对理由、未知项和置信度
→ 打开首页即可知道下一步最值得做什么以及为什么
→ 在雷达库可以查看全部候选和采集数据
```

### B. 日常可用 MVP

累计预计 18～25 个工作日。

增加：

- Product 状态管理；
- Opportunity 转 Product；
- 与已有 Product 去重；
- 保存用户对推荐的接受、拒绝和原因；
- Signal 与 Product/Opportunity 的人工关联；
- 数据新鲜度、历史评分和变化原因；
- 关键数据的定时更新和增量重评；
- Run 记录和成本上限；
- 失败重试；
- 本地或单服务器运行；
- 研究报告版本、必要测试和备份。

### C. 真实反馈与社区连接器

按实际价值逐个增加，每类连接器约 2～5 个工作日。

增加：

- Google OAuth；
- Search Console 属性绑定；
- query/page 同步；
- 新 query 发现；
- GSC 衍生机会；
- 产品增长信号。
- X 最近帖子搜索；
- Reddit 或其他获批社区数据源；
- 用户反馈表单；
- 自动把重复抱怨聚合成新 Opportunity。

---

## 13. MVP 验收标准

- [ ] 首页能看到当前高优先级产品机会；
- [ ] 雷达库能查看全部 Opportunity，不只保存首页摘要；
- [ ] 雷达库支持按评分、平台、结论和状态排序筛选；
- [ ] 雷达库和产品库支持分页；
- [ ] 首页能看到已有产品及其状态；
- [ ] Product 和 Opportunity 支持 Web、iOS、Web+iOS；
- [ ] iOS 候选使用真实 App Store 搜索、评分、评论和竞品数据；
- [ ] 每个机会明确推荐 Web、iOS 或双平台并解释原因；
- [ ] 所有最终推荐都经过 AI 判断，而不是由简单规则直接决定；
- [ ] 每个推荐至少覆盖需求、痛点、意图、SERP、竞品、差异化、可行性和扩展性；
- [ ] 每个重要事实和结论都能回到具体证据；
- [ ] 每个推荐同时包含支持理由、反对理由和关键未知项；
- [ ] 每个推荐有 AI 置信度，证据不足时不会强行推荐；
- [ ] 每个推荐有预计开发时间；
- [ ] 每个推荐说明是否需要后端和外部 API；
- [ ] 每个推荐说明适合新产品、功能还是 Landing Page；
- [ ] 相似关键词不会产生多个重复产品；
- [ ] 推荐会与已有产品比较；
- [ ] Opportunity 可以转为 Product；
- [ ] Product 可以标记为开发中和已上线；
- [ ] 可以手工运行一次发现任务；
- [ ] 可以看到任务是否成功和花费；
- [ ] 可以保存用户对推荐结果的接受、拒绝和原因；
- [ ] 可以手工添加产品点子并触发完整调研；
- [ ] 可以手工添加现有产品并纳入重复检测和机会匹配；
- [ ] 可以粘贴或导入 Reddit、X、论坛、App 评论等 Signal；
- [ ] Signal 可以成为补充证据或生成新的 Opportunity；
- [ ] 所有采集数据都能在详情页或原始证据中查看；
- [ ] 数据更新后会产生新的评分版本并显示变化原因；
- [ ] 增加评分维度后可以重新计算历史 Opportunity；
- [ ] 真实 API 不可用时明确报错，不用 Mock 冒充结果；
- [ ] 可以在本地或一台服务器上运行。

质量目标：

```text
高优先级完整调研候选中，至少 60% 经人工判断有明确开发价值
明显重复建议不超过 1 个
所有事实性结论都能追溯到数据或来源
同一份证据重复运行时，结论不应发生无理由的大幅变化
单次运行成本可预估、可限制
```

---

## 14. 第一版明确不做

- 通用 SEO SaaS；
- 多用户和多租户；
- 复杂权限；
- 自动生成和发布产品代码；
- 自动购买域名；
- 自动创建 GitHub 项目；
- 多国家、多语言；
- 向量数据库；
- 大量报表和图表；
- 完整成本分析中心；
- 邮件、Slack、飞书通知；
- 为了完整而增加没有直接价值的页面。

---

## 15. 最终判断原则

任何功能开发前都问：

1. 它能不能让我更快知道下一个做什么？
2. 它能不能避免重复开发？
3. 它能不能提高推荐结果的可信度？

如果三个答案都是“不能”，第一版就不做。

数据库技术可以简单，任务可以先手工运行，但产品机会、Signal、证据和历史判断必须长期积累。

但调研维度、证据质量、正反论证和 AI 最终判断不能省略。

产品雷达的核心资产是不断更新的产品机会数据库，核心能力是：

**用充分调研和可靠判断，持续筛选出真正值得开发的产品。**
